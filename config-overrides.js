const { override } = require('customize-cra');

module.exports = override(
    // config => {
    //     if (config.mode === 'production') {
    //         config.output.publicPath = 'https://mywebsite.com/';
    //     }
    //     return config;
    // }
);
